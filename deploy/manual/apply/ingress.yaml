apiVersion: extensions/v1beta1
kind: Ingress
metadata:
  name: 'pickly-backend-dev'
  namespace: 'default'
  labels:
    project: 'pickly'
    environment: 'dev'
    componenet: 'backend'
    application: 'pickly-backend-dev'
    version: '0.1'
  annotations:
    kubernetes.io/ingress.class: nginx
    nginx.ingress.kubernetes.io/proxy-body-size: 20m
spec:
  rules:
    - host: 'pickly-backend-dev.m3ntorship.net'
      http:
        paths:
          - backend:
              serviceName: 'pickly-backend-dev'
              servicePort: 80
            path: /
    - host: 'pickly-backend-dev2.m3ntorship.net'
      http:
        paths:
          - backend:
              serviceName: 'pickly-backend-dev'
              servicePort: 80
            path: /
    - host: 'pickly-backend-dev.m3ntorship.com'
      http:
        paths:
          - backend:
              serviceName: 'pickly-backend-dev'
              servicePort: 80
            path: /
    - host: 'pickly-backend-dev2.m3ntorship.com'
      http:
        paths:
          - backend:
              serviceName: 'pickly-backend-dev'
              servicePort: 80
            path: /